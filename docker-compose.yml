version: '3.1'

services:
  url_shortener:
    build:
      context: .
    restart: always
    environment:
      # App
      PORT: 3000
      BASE_URL: https://goi.is
    networks:
      - apps

networks:
  apps:
    external:
      name: apps
